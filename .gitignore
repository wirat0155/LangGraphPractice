# -----------------------------------------------------------------------------
# 1. ข้อมูลลับและความปลอดภัย (Secrets and Security)
# -----------------------------------------------------------------------------
# LangGraph มักจะใช้ API Keys หรือข้อมูลเฉพาะอื่นๆ ในไฟล์ .env
# ห้าม commit ไฟล์นี้โดยเด็ดขาด!
.env
.flaskenv # หากใช้ Flask

# -----------------------------------------------------------------------------
# 2. ไฟล์ Build/Output และ Cache
# -----------------------------------------------------------------------------
# โฟลเดอร์ที่เก็บ artifacts หรือ output ที่สร้างขึ้นขณะรันโปรแกรม
/dist/
/build/
/out/
/target/

# ไฟล์แคชและ metadata ของ Python
__pycache__/
*.pyc
*.pyd
*.pyo

# -----------------------------------------------------------------------------
# 3. Virtual Environment (Python)
# -----------------------------------------------------------------------------
# โฟลเดอร์ virtual environment ที่ใช้เก็บ dependencies (ชื่อที่พบบ่อย)
/venv
/env
/virtualenv
/myenv

# -----------------------------------------------------------------------------
# 4. Dependencies (Python/Node.js)
# -----------------------------------------------------------------------------
# ไฟล์/โฟลเดอร์สำหรับ dependency management
# Python
pip-log.txt
*.egg-info/
.pytest_cache/

# Node.js (หากใช้ร่วมกับ Frontend หรือเป็นโปรเจกต์ JS)
/node_modules

# -----------------------------------------------------------------------------
# 5. ข้อมูลเฉพาะ IDE และ OS
# -----------------------------------------------------------------------------
# IntelliJ / PyCharm / VS Code
.idea/
.vscode/

# macOS
.DS_Store

# Windows
Thumbs.db